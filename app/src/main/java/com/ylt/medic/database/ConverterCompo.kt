package com.ylt.medic.database

import androidx.room.TypeConverter
import com.ylt.medic.database.model.Compo
import java.util.*

class ConverterCompo {
    /*
    @TypeConverter
    fun fromCompo(value: String?): Compo? {
        return if (value == null) null else Compo(value)
    }

    @TypeConverter
    fun toCompo(compo: Compo?): String? {
        return compo?.toString()
    }
     */
}